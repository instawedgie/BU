"""
Used to remove clutter from the map script (which is already a nightmare)
"""

def draw_edge_arrow(node1, node2):
    """
    Used to draw an arrow between nodes (for debugging the graph)
    :param node1:
    :param node2:
    :return:
    """